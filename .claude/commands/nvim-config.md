Analyze and describe the current Neovim configuration project. Read the skill file at `.claude/skills/nvim-config-guide.md` for the complete reference, then provide a summary covering:

1. **Project overview** — framework, colorscheme, key design choices
2. **File structure** — how the config is organized
3. **Plugin inventory** — all 56 plugins grouped by category with their roles
4. **Custom configuration highlights** — what makes this config unique vs stock LazyVim
5. **Key bindings** — custom overrides and additions beyond LazyVim defaults
6. **Language support matrix** — LSP, formatter, debugger per language
7. **Notable design decisions** — architectural choices and their rationale

Use the skill file as the authoritative source. If anything seems outdated, verify against the actual files before reporting.